export enum UserRouteCode{
    CREATE,
    UPDATE_TOKEN,
    ADD_TOKEN,
    REMOVE_TOKEN,
    CHANGE_NAME,
    REMOVE_USER
}


export enum ChannelRouteCode {
    CREATE,
    UPDATE_KEY,
    CHANGE_PASS,
    ADD_USER
}